name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        python-version: ["3.11"]
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-dev.txt
    
    - name: Compile Python files
      run: |
        python -m compileall src
    
    - name: Run tests
      run: |
        pytest -q
    
    - name: Run preflight checks
      env:
        SUPABASE_URL: "https://test.supabase.co"
        SUPABASE_KEY: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJvbGUiOiJzZXJ2aWNlX3JvbGUiLCJyZWYiOiJ0ZXN0IiwiZXhwIjoxOTgzODEyOTk2fQ.test"
        GOOGLE_CLIENT_ID: "test.apps.googleusercontent.com"
        GOOGLE_CLIENT_SECRET: "test_secret_1234567890"
        GBP_REFRESH_TOKEN: "1//test_refresh_token_dummy_value"
        JOB_LIST: "test.job"
        PYTHONPATH: "src"
      run: |
        python -m automation_hub.runners.preflight
